title: Item
@extends:
    type: default
    context: blueprints://pages

form:
  fields:
    tabs:
      fields:
        hypertext:
          fields:
            columns:
              fields:
                

